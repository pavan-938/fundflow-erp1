# Seeds

Executable seed lives in `backend/src/db/seed.ts` so passwords are bcrypt-hashed.

```bash
cd backend
npm run seed
```

This folder is reserved for optional SQL fixtures. Prefer the TypeScript seeder for FundFlow.
